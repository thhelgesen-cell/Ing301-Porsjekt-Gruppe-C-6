info:
  name: device
  type: http
  seq: 8

http:
  method: GET
  url: http://127.0.0.1:8000/smarthouse/device
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
